services:
  - type: web
    name: deepmala-portfolio
    runtime: static
    buildCommand: echo "Static portfolio - no build step"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
